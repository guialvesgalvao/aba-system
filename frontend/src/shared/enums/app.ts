export enum SystemRoutes {
  ALL = "*",
  HOME = "/",
  ORDERS = "/orders",
  PRODUCTS = "/products",
  ORIGINS = "/origins",
  ORDERS = "/orders",
  DELIVERY_PERSONS = "/delivery-persons",
  SUPPLIERS = "/suppliers",
  CUSTOMERS = "/customers",
}
